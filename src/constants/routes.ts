export enum authRoutes {
    login = 'Login',
    passwordRecovery = 'PasswordRecovery'
};

export enum AppStacks {
    systemStack = 'SystemStack',
    historicalDataStack = 'HistoricalDataStack',
    reportStack = 'reportStack',
    editProfile = 'EditProfile',
}

export enum SystemStackRoutes {
    system = 'SystemBalls',
    systemDaily = 'SystemDaily',
    systemBalance = 'SystemBalance'
}

export enum HistoricalDataStackRoutes {
    daily = 'Daily',
    monthly = 'Monthly',
    annual = 'Annual'
}

export enum ReportStackRoutes {
    report = 'report'
}

export enum EditProfileStackRoutes {
    EditProfile = 'edit_profile'
}
